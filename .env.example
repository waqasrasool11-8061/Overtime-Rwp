# Overtime System Environment Configuration
PORT=3000
NODE_ENV=development

# Allowed Origins for CORS (comma-separated). Add your live domain here when hosted:
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000

# Session Secret Key
SESSION_SECRET=pr_rwp_overtime_session_secret_change_in_prod
